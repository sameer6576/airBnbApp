spring.datasource.url=jdbc:h2:mem:airbnb_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# Postgres TEXT[] in V1 is not valid H2. Unit/WebMvc tests keep Hibernate DDL.
spring.flyway.enabled=false

server.servlet.context-path=/api/v1

jwt.secretKey=ThisIsASecretKeyThatIsAtLeast32CharactersLong123456

frontend.url=http://localhost:8080
stripe.secret.key=sk_test_dummy
stripe.webhook.secret=whsec_dummy

app.seed.enabled=false
app.holidays.dates[0]=01-01
app.holidays.dates[1]=12-25
